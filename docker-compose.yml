version: '3.8'
services:
  myapp:
    build:
      context: .
      dockerfile: Dockerfile
#    depends_on:
#      - db
    ports:
      - "8080:8080"
    networks:
      - spring-network


networks:
  spring-network:
    driver: bridge
